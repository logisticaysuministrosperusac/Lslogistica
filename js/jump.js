function mostrarProducto(archivo) {
    // Redireccionar a la página de productos
    window.location.href = 'prueba_2.html';
  
    // Guardar el archivo que se debe mostrar en localStorage
    localStorage.setItem('producto', archivo);
  }
  